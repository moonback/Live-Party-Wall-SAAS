# Partywall - Cursor Rules

## 📋 Contexte du Projet

Partywall est une application web React en temps réel qui permet aux invités d'un événement de partager instantanément leurs photos sur grand écran. L'application utilise l'IA (Google Gemini) pour modérer, améliorer et légender automatiquement chaque photo.

**Objectifs principaux** :
- Expérience photobooth interactive pour événements
- Affichage temps réel sur grand écran
- Modération automatique par IA
- Administration complète pour les organisateurs

---

## 🛠 Stack Technique

### Frontend
- **React 19.2** : Composants fonctionnels avec Hooks, Suspense, Lazy Loading
- **TypeScript 5.8** : Typage strict, éviter `any`
- **Vite 6.2** : Build tool avec HMR
- **Tailwind CSS 4.1** : Utility-first CSS
- **Lucide React** : Icônes

### Backend & Infrastructure
- **Supabase** : BaaS (PostgreSQL, Storage, Realtime, Auth)
  - PostgreSQL : Base de données relationnelle avec RLS
  - Storage : Stockage de fichiers (photos, cadres, avatars)
  - Realtime : Synchronisation temps réel via WebSockets
  - Auth : Authentification JWT avec email/password
- **Google Gemini 3 Flash** : IA pour modération et légendes
  - Modération de contenu automatique
  - Génération de légendes personnalisées
  - Analyse de qualité d'images

### Outils & Bibliothèques
- **JSZip** : Export ZIP de photos
- **File Saver** : Téléchargement de fichiers côté client
- **QRCode React** : Génération de QR codes
- **Framer Motion** : Animations fluides et performantes
- **Face-api.js** : Reconnaissance faciale
- **@tanstack/react-virtual** : Virtualisation pour performances

---

## 📐 Architecture

### Structure des Dossiers
```
components/     # Composants UI React (un composant par fichier)
services/       # Logique métier et intégrations API
utils/          # Utilitaires (validation, filtres, overlays)
context/        # Contextes React (ToastContext)
types.ts        # Types TypeScript partagés
constants.ts    # Constantes globales
```

### Patterns Architecturaux
- **Service Layer Pattern** : Toute la logique métier dans `/services`, les composants restent "stupides"
- **Context API** : Pour l'état global partagé (Event, Photos, Settings, Auth, Toast)
- **Lazy Loading** : Tous les composants principaux sont lazy-loaded avec React.lazy()
- **Type Safety** : TypeScript strict, types définis dans `types.ts`
- **Multi-événements** : Architecture SaaS avec table `events` et `event_id` partout
- **RLS (Row Level Security)** : Sécurité au niveau des lignes dans Supabase

---

## 💻 Conventions de Code

### TypeScript
- ✅ **Toujours utiliser TypeScript** pour nouveau code
- ✅ **Éviter `any`** : Utiliser `unknown` ou types explicites
- ✅ **Interfaces pour objets complexes** : Définir dans `types.ts` si partagé
- ✅ **JSDoc pour fonctions complexes** : Documenter les paramètres et retours

**Exemple** :
```typescript
/**
 * Upload une photo vers Supabase Storage
 * @param base64Image - Image en base64
 * @param caption - Légende de la photo
 * @param author - Nom de l'auteur
 * @returns Promise résolue avec l'objet Photo créé
 */
export const addPhotoToWall = async (
  base64Image: string,
  caption: string,
  author: string
): Promise<Photo> => {
  // ...
};
```

### React
- ✅ **Composants fonctionnels uniquement** : Pas de classes
- ✅ **Hooks pour état et effets** : `useState`, `useEffect`, `useContext`
- ✅ **Props typées** : Toujours définir une interface pour les props
- ✅ **Nommage PascalCase** : `GuestUpload`, `WallView`

**Exemple** :
```typescript
interface GuestUploadProps {
  onPhotoUploaded: (photo: Photo) => void;
  onBack: () => void;
}

export const GuestUpload: React.FC<GuestUploadProps> = ({
  onPhotoUploaded,
  onBack
}) => {
  // ...
};
```

### Nommage
- **Fichiers** : `camelCase.tsx` (composants), `camelCase.ts` (services/utils)
- **Composants** : `PascalCase`
- **Fonctions/Variables** : `camelCase`
- **Constantes** : `UPPER_SNAKE_CASE`
- **Types/Interfaces** : `PascalCase`

### Formatage
- **Indentation** : 2 espaces
- **Guillemets** : Simple quotes pour JS/TS, double pour JSX
- **Point-virgule** : Oui
- **Trailing commas** : Oui dans objets/arrays multilignes

---

## 🏗️ Patterns de Développement

### Services
- **Isolation** : Chaque service encapsule une responsabilité (photoService, geminiService, etc.)
- **Erreurs** : Gérer les erreurs avec try/catch, retourner des fallbacks pour services IA
- **Validation** : Valider les paramètres d'entrée avant traitement

**Exemple** :
```typescript
export const addPhotoToWall = async (
  base64Image: string,
  caption: string,
  author: string
): Promise<Photo> => {
  if (!isSupabaseConfigured()) {
    throw new Error("Supabase n'est pas configuré");
  }
  
  try {
    // Logique métier
  } catch (error) {
    console.error("Error in addPhotoToWall:", error);
    throw error instanceof Error ? error : new Error("Erreur lors de l'upload");
  }
};
```

### Composants
- **Séparation des responsabilités** : UI dans composants, logique dans services
- **Props claires** : Nommer les props de manière explicite
- **Gestion d'état** : État local avec `useState`, Context pour état global partagé
- **Lazy Loading** : Utiliser `React.lazy()` pour composants lourds

**Exemple** :
```typescript
// Dans App.tsx
const GuestUpload = lazy(() => import('./components/GuestUpload'));

// Utilisation avec Suspense
<Suspense fallback={<LoadingSpinner />}>
  <GuestUpload onPhotoUploaded={handleUpload} onBack={handleBack} />
</Suspense>
```

### Gestion d'Erreurs
- **Services** : Try/catch avec logging et propagation
- **Composants** : Afficher des toasts via `ToastContext` pour erreurs utilisateur
- **Fallbacks** : Services IA retournent des valeurs "safe" par défaut

**Exemple** :
```typescript
try {
  const caption = await generateImageCaption(base64);
  return caption;
} catch (error) {
  console.error("Error generating caption:", error);
  return "Party time! 🎉"; // Fallback
}
```

---

## 🔒 Sécurité

### Validation
- ✅ **Valider tous les inputs** : Fichiers (taille, type MIME), texte (longueur, caractères)
- ✅ **Sanitization** : Nettoyer les inputs pour prévenir XSS
- ✅ **RLS Supabase** : Toujours vérifier que RLS est activé sur nouvelles tables

**Exemple** :
```typescript
export const validateImageFile = (file: File): { valid: boolean; error?: string } => {
  if (file.size > MAX_FILE_SIZE) {
    return { valid: false, error: 'Le fichier est trop volumineux (max 10MB)' };
  }
  if (!ALLOWED_IMAGE_TYPES.includes(file.type)) {
    return { valid: false, error: 'Type de fichier non autorisé' };
  }
  return { valid: true };
};
```

### Variables d'Environnement
- ✅ **Préfixe `VITE_`** : Pour variables accessibles côté client
- ✅ **Ne jamais commiter `.env`** : Déjà dans `.gitignore`
- ✅ **Validation au démarrage** : Vérifier que les credentials sont présents

---

## 🧪 Tests (À Implémenter)

### Structure Future
- **Unit Tests** : Services avec mocks Supabase/Gemini
- **Integration Tests** : Flux complets (upload → affichage)
- **E2E Tests** : Scénarios utilisateur (Playwright/Cypress)

### Bonnes Pratiques
- Tester les cas d'erreur (API down, validation échouée)
- Mocker les appels externes (Supabase, Gemini)
- Tester les edge cases (fichiers très gros, réseau lent)

---

## 📚 Documentation

### Code Comments
- ✅ **JSDoc pour fonctions publiques** : Services, utilitaires complexes
- ✅ **Commentaires pour "pourquoi"** : Expliquer les décisions, pas le "quoi"
- ✅ **Éviter les commentaires évidents** : Le code doit être auto-explicatif

### Documentation Utilisateur
- ✅ **Mettre à jour README.md** : Si nouvelle fonctionnalité utilisateur
- ✅ **Mettre à jour ARCHITECTURE.md** : Si modification architecturale
- ✅ **Mettre à jour API_DOCS.md** : Si nouveau service ou endpoint
- ✅ **Mettre à jour DB_SCHEMA.md** : Si modification base de données

---

## 🚫 Anti-Patterns à Éviter

### ❌ À Ne Pas Faire
- ❌ **Logique métier dans composants** : Extraire dans services
- ❌ **Props drilling excessif** : Utiliser Context si nécessaire
- ❌ **État global inutile** : Préférer état local quand possible
- ❌ **Re-renders inutiles** : Utiliser `useMemo`, `useCallback` si nécessaire
- ❌ **Appels API dans composants** : Utiliser les services
- ❌ **Types `any`** : Toujours typer explicitement
- ❌ **Composants trop gros** : Extraire en sous-composants
- ❌ **Duplication de code** : Extraire en fonctions/services réutilisables

### ✅ À Faire
- ✅ **Composants petits et focalisés** : Une responsabilité par composant
- ✅ **Services réutilisables** : Logique partagée dans services
- ✅ **Types partagés** : Définir dans `types.ts`
- ✅ **Constantes centralisées** : Définir dans `constants.ts`
- ✅ **Gestion d'erreurs robuste** : Try/catch, fallbacks, logging

---

## 🔄 Workflows Communs

### Ajouter une Nouvelle Fonctionnalité
1. **Créer le service** dans `/services` si logique métier
2. **Créer le composant** dans `/components` si UI
3. **Définir les types** dans `types.ts` si nouveaux types
4. **Ajouter les constantes** dans `constants.ts` si nécessaire
5. **Intégrer dans App.tsx** avec lazy loading si composant principal
6. **Tester manuellement** : Upload, affichage, erreurs
7. **Mettre à jour la documentation** si nécessaire

### Créer un Nouveau Service
1. **Créer le fichier** dans `/services/nomService.ts`
2. **Exporter les fonctions** avec types explicites
3. **Gérer les erreurs** avec try/catch et fallbacks
4. **Valider les inputs** avant traitement
5. **Documenter avec JSDoc** si fonction complexe
6. **Tester manuellement** avec différents cas

### Ajouter une Table Supabase
1. **Créer le script SQL** : `supabase_nom_table_setup.sql`
2. **Ajouter `event_id`** : Toutes les tables doivent avoir `event_id UUID REFERENCES events(id) ON DELETE CASCADE`
3. **Définir RLS** : Politiques SELECT, INSERT, UPDATE, DELETE avec filtres `event_id`
4. **Créer les index** : Sur `event_id` et colonnes fréquemment requêtées
5. **Activer Realtime** : Si nécessaire pour temps réel (Dashboard Supabase > Replication)
6. **Créer le service** : Fonctions CRUD dans `/services/nomService.ts`
7. **Mettre à jour DB_SCHEMA.md** : Documenter la nouvelle table
8. **Tester les permissions** : Vérifier RLS fonctionne correctement

### Modifier l'UI
1. **Identifier le composant** à modifier
2. **Vérifier les props** : S'assurer que les types sont corrects
3. **Modifier le JSX** : Utiliser Tailwind CSS pour le styling
4. **Tester responsive** : Vérifier sur mobile et desktop
5. **Tester les interactions** : Clics, hovers, transitions

### Créer un Nouveau Hook
1. **Créer le fichier** dans `/hooks/nomHook.ts`
2. **Utiliser les conventions** : Préfixe `use` (ex: `useMyFeature`)
3. **Typer les retours** : Définir le type de retour explicitement
4. **Gérer les dépendances** : Utiliser `useEffect` avec dépendances correctes
5. **Documenter avec JSDoc** : Expliquer l'usage et les paramètres
6. **Exemple** :
```typescript
/**
 * Hook personnalisé pour [description]
 * @param param1 - Description du paramètre
 * @returns Objet avec les valeurs retournées
 */
export const useMyFeature = (param1: string) => {
  const [state, setState] = useState<StateType>(initialState);
  
  useEffect(() => {
    // Logique du hook
  }, [param1]);
  
  return { state, action: () => {} };
};
```

### Utiliser un Worker
1. **Créer le worker** dans `/workers/nomWorker.worker.ts`
2. **Exporter les fonctions** : Fonctions qui seront appelées depuis le main thread
3. **Utiliser dans le code** : Créer une instance du worker et communiquer via messages
4. **Exemple** :
```typescript
// Dans le worker
self.onmessage = (e) => {
  const { imageData, options } = e.data;
  // Traitement
  self.postMessage({ result: processedImage });
};

// Dans le code principal
const worker = new Worker(new URL('./workers/imageWorker.worker.ts', import.meta.url));
worker.postMessage({ imageData, options });
worker.onmessage = (e) => {
  const { result } = e.data;
  // Utiliser le résultat
};
```

---

## 📦 Dépendances à Favoriser

### Pour des Tâches Communes
- **Validation** : Utiliser les fonctions dans `/utils/validation.ts`
- **Filtres d'image** : Utiliser `/utils/imageFilters.ts`
- **Overlays/Cadres** : Utiliser `/utils/imageOverlay.ts`
- **Toasts** : Utiliser `ToastContext` pour notifications
- **Supabase** : Utiliser `supabaseClient.ts` pour toutes les opérations DB
- **Compression** : Utiliser `useImageCompression` hook pour optimiser les images
- **Debounce** : Utiliser `useDebounce` hook pour limiter les appels API
- **Logger** : Utiliser `logger` de `/utils/logger.ts` pour le logging structuré

### Services Disponibles (29 services)
- **photoService.ts** : CRUD photos, likes, réactions, upload
- **eventService.ts** : Gestion événements, organisateurs
- **guestService.ts** : Gestion invités, blocage
- **geminiService.ts** : Modération IA, légendes, analyse qualité
- **settingsService.ts** : Paramètres d'événement
- **battleService.ts** : Battles photos
- **exportService.ts** : Export ZIP
- **aftermovieService.ts** : Génération vidéos timelapse
- **aftermovieShareService.ts** : Partage et upload aftermovies
- **aftermovieAIService.ts** : Sélection IA pour aftermovies
- **photoboothService.ts** : Upload photobooth avec traitement complet
- **faceRecognitionService.ts** : Reconnaissance faciale
- **faceStorageService.ts** : Stockage descripteurs faciaux
- **frameService.ts** : Gestion cadres décoratifs
- **localFramesService.ts** : Cadres locaux
- **backgroundService.ts** : Gestion fonds d'écran
- **gamificationService.ts** : Badges, classements
- **rgpdService.ts** : Gestion RGPD (consentement, export, suppression)
- **licenseService.ts** : Gestion licences
- **licenseSupabaseClient.ts** : Client Supabase pour licences
- **translationService.ts** : Traduction multilingue
- **aiService.ts** : Service IA général
- **aiModerationService.ts** : Modération IA spécialisée
- **autoBattleService.ts** : Battles automatiques
- **applauseDetectionService.ts** : Détection applaudissements
- **socialShareService.ts** : Partage social
- **eventContextService.ts** : Gestion contexte événement
- **supabaseClient.ts** : Client Supabase principal

### Hooks Personnalisés Disponibles
- **useCamera.ts** : Gestion caméra (accès, contraintes, capture)
- **useImageCompression.ts** : Compression d'images optimisée
- **useDebounce.ts** : Debounce pour optimisations
- **useIsMobile.ts** : Détection mobile/desktop
- **useFaceRecognition.ts** : Reconnaissance faciale
- **useImageOrientation.ts** : Détection orientation images
- **useImageProcessing.ts** : Traitement d'images
- **useInView.ts** : Détection visibilité (Intersection Observer)
- **useLazyImage.ts** : Chargement lazy des images
- **useOptimizedSubscription.ts** : Abonnements Realtime optimisés
- **useReducedMotion.ts** : Détection préférence mouvement réduit
- **useSwipe.ts** : Gestes swipe
- **useVideoRecording.ts** : Enregistrement vidéo
- **useLicenseFeatures.ts** : Vérification fonctionnalités licence
- **useAdaptiveCameraResolution.ts** : Résolution caméra adaptative
- **useCameraZoom.ts** : Zoom caméra
- **hooks/wall/** : Hooks spécifiques au mur
- **hooks/queries/** : Hooks React Query (usePhotosQuery, usePhotosRealtime)

### Workers Disponibles
- **faceRecognition.worker.ts** : Reconnaissance faciale (face-api.js)
- **imageCompression.worker.ts** : Compression d'images
- **imageEnhancement.worker.ts** : Amélioration qualité
- **imageFilters.worker.ts** : Application de filtres
- **imageOverlay.worker.ts** : Ajout de cadres décoratifs
- **aftermovieWorker.worker.ts** : Génération aftermovies

### Bibliothèques à Éviter
- ❌ **État global lourd** : Redux/Zustand (pas nécessaire actuellement)
- ❌ **Formulaires complexes** : React Hook Form (pas encore nécessaire)
- ❌ **Routing** : React Router (routing manuel actuellement)
- ❌ **State management externe** : Context API suffit pour la taille actuelle

---

## 🎯 Objectifs de Qualité

### Code Quality
- **Lisibilité** : Code auto-explicatif, noms clairs
- **Maintenabilité** : Structure claire, documentation
- **Performance** : Lazy loading, optimisation des re-renders
- **Sécurité** : Validation, sanitization, RLS

### Expérience Utilisateur
- **Feedback** : Toasts pour toutes les actions importantes
- **Performance** : Chargement rapide, transitions fluides
- **Accessibilité** : Labels, ARIA (à améliorer)
- **Mobile-first** : Interface responsive et optimisée mobile

---

## 🔍 Vérifications Avant Commit

- [ ] Code suit les conventions (nommage, formatage)
- [ ] Types TypeScript corrects (pas d'erreurs, pas de `any`)
- [ ] Gestion d'erreurs appropriée (try/catch, fallbacks pour IA)
- [ ] Tests manuels effectués (upload, affichage, erreurs)
- [ ] Documentation mise à jour si nécessaire (README, ARCHITECTURE, API_DOCS, DB_SCHEMA)
- [ ] Pas de `console.log` oubliés (utiliser `logger` de `/utils/logger.ts`)
- [ ] Pas de code commenté mort
- [ ] Variables d'environnement vérifiées (pas de secrets dans le code)
- [ ] RLS Supabase vérifié si nouvelle table
- [ ] `event_id` ajouté si nouvelle table (architecture multi-événements)

---

## 🔄 Workflows Spécifiques

### Ajouter une Fonctionnalité Multi-Tenant
1. **Vérifier `event_id`** : Toutes les tables doivent avoir `event_id`
2. **Filtrer par événement** : Toutes les requêtes doivent filtrer par `event_id`
3. **RLS par événement** : Les politiques RLS doivent vérifier l'appartenance à l'événement
4. **EventContext** : Utiliser `useEvent()` pour obtenir l'événement actif
5. **Exemple** :
```typescript
const { currentEvent } = useEvent();
const photos = await getPhotos(currentEvent.id);
```

### Intégrer une Fonctionnalité IA
1. **Utiliser geminiService** : Pour modération, légendes, tags
2. **Gérer les erreurs** : Toujours avoir un fallback si l'IA échoue
3. **Respecter les limites** : Ne pas surcharger l'API Gemini
4. **Exemple** :
```typescript
try {
  const caption = await generateImageCaption(base64Image, eventContext);
  return caption;
} catch (error) {
  logger.error('IA caption failed', error);
  return 'Moment magique ! ✨'; // Fallback
}
```

### Ajouter une Fonctionnalité Realtime
1. **Activer Realtime** : Dans Supabase Dashboard > Database > Replication
2. **Utiliser usePhotosRealtime** : Hook pour subscriptions automatiques
3. **Gérer les subscriptions** : Nettoyer les subscriptions au démontage
4. **Exemple** :
```typescript
useEffect(() => {
  const subscription = supabase
    .channel('photos')
    .on('postgres_changes', {
      event: 'INSERT',
      schema: 'public',
      table: 'photos',
      filter: `event_id=eq.${eventId}`
    }, (payload) => {
      addPhoto(payload.new as Photo);
    })
    .subscribe();
  
  return () => {
    subscription.unsubscribe();
  };
}, [eventId]);
```

## 📖 Ressources

- **Documentation Supabase** : https://supabase.com/docs
- **Documentation React** : https://react.dev
- **Documentation Gemini** : https://ai.google.dev/docs
- **Documentation Vite** : https://vitejs.dev
- **Documentation Tailwind** : https://tailwindcss.com/docs
- **Documentation Electron** : https://www.electronjs.org/docs
- **Documentation TypeScript** : https://www.typescriptlang.org/docs

## 📚 Documentation du Projet

Consulter les documents suivants pour plus de détails :
- **[README.md](./README.md)** : Documentation principale
- **[ARCHITECTURE.md](./ARCHITECTURE.md)** : Architecture détaillée
- **[API_DOCS.md](./API_DOCS.md)** : Documentation complète des services
- **[DB_SCHEMA.md](./DB_SCHEMA.md)** : Schéma de base de données
- **[ROADMAP.md](./ROADMAP.md)** : Feuille de route
- **[CONTRIBUTING.md](./CONTRIBUTING.md)** : Guide de contribution

---

**Dernière mise à jour** : 2026-01-15
